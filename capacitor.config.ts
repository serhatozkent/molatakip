import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.erc.molatakip",
  appName: "molatakip",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
